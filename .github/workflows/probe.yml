name: Ably reusable workflow OIDC trust probe

on:
  push:
    branches:
      - main

permissions: {}

jobs:
  probe:
    permissions:
      contents: read
      deployments: write
      id-token: write
    uses: ably/features/.github/workflows/sdk-features.yml@6b3fc7a8ede2ebdd7a6325314f3a96c6466f1453
    with:
      repository-name: sdk-upload-action
    secrets:
      ABLY_AWS_ACCOUNT_ID_SDK: ${{ '203461409171' }}
